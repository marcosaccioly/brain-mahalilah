{
  "name": "brain-mahalilah",
  "version": "1.0.0",
  "description": "IA Padma — Instituto Maha Lilah",
  "private": true,
  "type": "module",
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix bot",
    "server": "npm start --prefix server",
    "server:dev": "npm run dev --prefix server",
    "bot": "npm start --prefix bot",
    "bot:dev": "npm run dev --prefix bot",
    "start": "npm run server"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.39.0"
  }
}
